version: 2.1

jobs:
  node-test:
    docker:
      - image: node:14
    resource_class: 'medium+'
    steps:
      - checkout
      - run: |
          node -v
          npm -v
      - run: exit 0
          

workflows:
  main:
    jobs:
      - node-test
